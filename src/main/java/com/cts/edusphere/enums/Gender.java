package com.cts.edusphere.enums;

/**
 * Represents the gender identity options available for a user profile.
 */
public enum Gender {
    /** The user identifies as male. */
    MALE,
    /** The user identifies as female. */
    FEMALE,
    /** The user has chosen not to disclose their gender identity. */
    PREFER_NOT_TO_SAY
}
