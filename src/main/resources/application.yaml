server:
  port: 9090

spring:
  application:
    name: edusphere
  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
